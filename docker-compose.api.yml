version: '3.8'

services:
  app:
    image: php:8.2-fpm
    container_name: ${STACK_NAME}_app
    labels:
      - "version=2.0.0-beta"
      - "branch=develop"
      - "environment=development"
      - "release=beta"
      - "feature=debug-mode"
