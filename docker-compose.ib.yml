version: '3.8'

services:
  ibank:
    image: nginx:alpine
    container_name: ${STACK_NAME}_ibank
    ports:
      - "80"
    restart: unless-stopped
    labels:
      - "sandbox=${STACK_NAME}"
      - "type=ibank"
